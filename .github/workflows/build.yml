name: 'build'

on: [push, pull_request]

jobs:
  build:
  #   strategy:
  #     matrix:
  #       idf_ver: ["v3.3", "v4.0", "v4.1", "v4.2", "v4.3"]
  #       idf_target: ["esp32"]
  #       include:
  #         - idf_ver: "v4.2"
  #           idf_target: esp32s2
  #         - idf_ver: "v4.3"
  #           idf_target: esp32c3
    runs-on: ubuntu-18.04
    # container: espressif/idf:release-${{ matrix.idf_ver }}
    steps:
      - name: tmp
        shell: bash
        run: |
          df -h
          sudo rm -rf /usr/share/dotnet
          df -h
          

      # - uses: actions/checkout@v1
      #   with:
      #     submodules: recursive
      # - name: Build for ESP32
      #   env:
      #     IDF_TARGET: ${{ matrix.idf_target }}
      #   shell: bash
      #   run: |
      #     . ${IDF_PATH}/export.sh
      #     cd example
      #     idf.py build
